# 交易猫看板抓取项目 · 交接文档

**更新日期:** 2026-08-17  
**仓库:** [https://github.com/Yuuki5311/Automatic](https://github.com/Yuuki5311/Automatic)  
**当前功能分支:** `feat/board-stats-local`（最新能力在此分支；`main` 可能落后）  
**Go module:** `github.com/example/jiaoyimao-scraper`（Go 1.26+）

---

## 1. 项目是做什么的

从**交易猫商户工作台**拉取各游戏「回收看板」统计（本月 MTD），落本地 JSON，再 upsert 到**飞书多维表格**看板表。

配套能力：

- 多店铺账号（Cookie 来自 leyoo 店铺列表拉取，非抓取时自动密码登录）
- 定时每日抓取 + Web / GUI 状态面板
- Cookie 失效时重新拉列表；仍失败则禁用账号
- 「最近抓取」失败账号可**手工补录**指标到飞书（不重跑抓取）

**当前阶段主路径是看板统计，不是历史订单明细同步**（`feishu.table_mapping` 为订单方案遗留配置）。

---

## 2. 运行方式（运维必读）

### 2.1 推荐：GUI（Windows）

发布目录常见形态：`dist/jiaoyimao-scraper/`（或拷贝后的同级结构）

```
jiaoyimao-scraper/
  gui.exe
  scraper.exe          # 可选备用入口
  configs/config.yaml  # 真实凭据，勿提交 Git
  data/                # 运行时生成：accounts、cookies、stats、log
```

- 双击 `gui.exe`：加载同级 `configs/config.yaml`，启动 cron + 内嵌 WebView 面板。
- **依赖 Microsoft Edge WebView2 Runtime**。没有则窗口起不来；可用下方无 GUI 方案。
- 启动失败会写 `startup_error.txt`，并尽量暂停便于查看错误。



### 2.2 无 GUI：守护进程 + 浏览器面板

适合脚本机 / 未装 WebView2：

```bat
scraper.exe -daemon -web -config configs\config.yaml
```

浏览器打开 `http://127.0.0.1:8080`（见 `web.addr`）。

### 2.3 单次抓取

```bat
scraper.exe -once -config configs\config.yaml
```

或默认（非 `-daemon`）也是跑一轮后退出。

### 2.4 定时规则

- 配置项：`scraper.cron_expr`（标准 cron，例：`20 9 * * *` = 每天 9:20）
- GUI 面板可改「每天 HH:MM」并写回配置
- **进程必须常驻**；退出后不会再定时跑
- 若上一轮仍在跑，本轮 cron 会 Skip（`SkipIfStillRunning`）

---



## 3. 配置与密钥


| 文件                            | 说明                                     |
| ----------------------------- | -------------------------------------- |
| `configs/config.yaml.example` | 模板，可提交                                 |
| `configs/config.yaml`         | 生产配置，**已 gitignore**                   |
| `configs/config.test.yaml`    | 测试占位，飞书 ID 须 `tblTEST*` / `bascnTEST*` |


必填要点：

- **飞书:** `app_id` / `app_secret` / `bitable_id` / `board_table_id`
- **抓取:** `scraper.mode` 建议 `api`；`scraper.games` 列表决定抓哪些游戏
- **Leyoo Cookie 解密:** `credential.aes_key`（16 字节）或环境变量 `THIRDPARTYSYNC_AES_KEY`，**不要提交密钥**
- **日志:** `log.file`（默认 `./data/scraper.log`）

复制模板：

```bat
copy configs\config.yaml.example configs\config.yaml
```

---



## 4. 数据目录约定

相对 exe / 工作目录的 `data/`（由 `jiaoyimao.cookie_path` 推导）：


| 路径                         | 用途                  |
| -------------------------- | ------------------- |
| `data/accounts.json`       | 多账号库（启用/禁用、店铺信息、状态） |
| `data/cookies_*.json` 等    | 各账号 Cookie          |
| `data/scrape_history.json` | 「最近抓取」历史            |
| `data/stats/`              | 看板快照 JSON           |
| `data/scraper.log`         | 运行日志                |


换机时：**整包拷贝** `configs` **+** `data`，再拷贝新版 exe。

---



## 5. 核心业务规则（交接重点）



### 5.1 抓取时间范围

- 交易猫 API：`time = "month"`（本月月初至今，与后台「本月」一致）
- 常量：`internal/scraper.BoardStatsTimeKey`



### 5.2 飞书日期与同月更新

- 快照 / 飞书「日期」字段 = **执行抓取的当天**（`ScrapeDayDate`）
- Upsert 匹配键 = `YYYY-MM` **+ 账号 + 游戏**（同月同行更新，改日期为本次抓取日；跨月新建）
- **不再删除**因游戏跳过等原因产生的飞书旧行（只 upsert）

实现：`internal/feishu/board_sync.go`

### 5.3 一轮抓取流程

```
启动/每轮开始 → SyncExistingSuppliers（全量同步店铺 Cookie）
  → 遍历 Enabled 账号（账号间随机等待 5–10s）
    → Cookie 无效则 Refresh（leyoo）→ 仍无效则禁用 + 历史「登录失败」
    → ScrapeBoardAll（各游戏看板 API）
    → 落盘 stats → SyncBoardStats → 写 scrape history
```

编排入口：`internal/pipeline/board.go`（GUI、cron、`POST /api/scrape` 共用）

### 5.4 失败语义（面板「最近抓取」）


| 情况                | 表现                   |
| ----------------- | -------------------- |
| Cookie/登录不可用      | 红：登录失败；账号可被禁用        |
| 有权限但确认查询后解析失败等硬错误 | 红：抓取 xx 失败；仍尽量保存成功游戏 |
| 无权限 / 无接口 / 未确认缺失 | **不算账号失败**（软跳过）      |




### 5.5 账号启用策略

- 用户**手动禁用**、或因「登录失败」禁用：拉列表后**保持禁用**
- 因 Cookie 类原因自动禁用：拉列表若拿到有效 Cookie，可**重新启用**



### 5.6 手工补录飞书

- 面板「最近抓取」点账号 → 选游戏、填指标（可部分空）
- 日期取历史分组日；提交 `POST /api/board/manual`
- 同键整行覆盖（未填指标对应字段清空）
- 设计见：`docs/superpowers/specs/2026-08-15-manual-board-supplement-design.md`

---



## 6. 代码地图

```
cmd/gui/          Wails GUI + 本地仪表盘 + cron
cmd/scraper/      CLI：-once / -daemon / -web
cmd/cookie-tool/  Cookie 辅助工具
cmd/debug/        调试入口

internal/
  pipeline/       看板一轮编排（最重要）
  scraper/        交易猫 API / 浏览器抓取
  feishu/         飞书 token、看板 upsert、手工行
  accounts/       多账号持久化
  accountsync/    leyoo 列表同步与 Cookie 刷新
  leyoo/          第三方店铺列表客户端
  auth/           Cookie 加载与校验、登录服务
  browser/        Rod 浏览器管理
  captcha/        滑块等（登录遗留路径）
  web/            HTTP 仪表盘与 API
  scrapehistory/  最近抓取历史
  statsstore/     本地看板 JSON
  config/         YAML + 定时 HH:MM 读写
  status/         运行态快照（给面板）

credentialcrypto/ leyoo 账号密码 AES 解密
browser-cat-slider/ 滑块轨迹相关库
docs/superpowers/ 设计稿与实现计划（按日期）
```



### 主要 HTTP API


| 方法   | 路径                                  | 作用       |
| ---- | ----------------------------------- | -------- |
| GET  | `/`                                 | 仪表盘      |
| GET  | `/api/status`                       | 状态 JSON  |
| POST | `/api/scrape`                       | 立即抓取（异步） |
| POST | `/api/schedule`                     | 改每日定时    |
| POST | `/api/accounts/pull`                | 拉取店铺列表   |
| POST | `/api/accounts/{id}/enable|disable` | 启停账号     |
| GET  | `/api/board/games`                  | 游戏列表     |
| POST | `/api/board/manual`                 | 手工补录     |


---



## 7. 开发与构建

```bat
go mod tidy
go test ./internal/scraper/ ./internal/feishu/ ./internal/pipeline/ ./internal/accountsync/ ./internal/web/ ./internal/config/ ./credentialcrypto/ ./cmd/gui/ -count=1
go build -o bin/scraper.exe ./cmd/scraper
go build -o bin/gui.exe ./cmd/gui
```

Makefile：`make deps` / `make build` / `make test`（类 Unix；Windows 可直接用上面命令）。

GUI 使用 Wails v3（见 `wails.json`）。发布时把 `gui.exe`（及可选 `scraper.exe`）与 `configs/`、空或已有 `data/` 一起打包；**勿把含密钥的** `config.yaml` **打进公开仓库**。

---



## 8. 相关设计文档（按主题）


| 主题       | 路径                                                                    |
| -------- | --------------------------------------------------------------------- |
| 看板本地落盘   | `docs/superpowers/specs/2026-08-13-board-stats-local-design.md`       |
| 多账号看板    | `docs/superpowers/specs/2026-08-13-multi-account-board-design.md`     |
| 飞书看板同步   | `docs/superpowers/specs/2026-08-13-feishu-board-stats-sync-design.md` |
| 抓取历史     | `docs/superpowers/specs/2026-08-14-scrape-history-design.md`          |
| 手工补录     | `docs/superpowers/specs/2026-08-15-manual-board-supplement-design.md` |
| Web 仪表盘  | `docs/superpowers/specs/2026-08-12-web-dashboard-logging-design.md`   |
| API 调研笔记 | `docs/api-research.md`                                                |


